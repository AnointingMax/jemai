/** Exhibitions — a placeholder until the frames for this section land. */
const AdminExhibitionsPage = () => (
  <section className="flex flex-col gap-2">
    <h1 className="text-text-primary text-2xl font-semibold">Exhibitions</h1>
    <p className="text-text-secondary text-body">Upcoming and past exhibitions, and their registrations.</p>
  </section>
);

export default AdminExhibitionsPage;
