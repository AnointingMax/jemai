/** Consultation requests — a placeholder until the frames for this section land. */
const AdminConsultationRequestsPage = () => (
  <section className="flex flex-col gap-2">
    <h1 className="text-text-primary text-2xl font-semibold">Consultation requests</h1>
    <p className="text-text-secondary text-body">Design consultation requests from the enquiry form.</p>
  </section>
);

export default AdminConsultationRequestsPage;
