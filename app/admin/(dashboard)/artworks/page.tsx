/** Artworks — a placeholder until the frames for this section land. */
const AdminArtworksPage = () => (
  <section className="flex flex-col gap-2">
    <h1 className="text-text-primary text-2xl font-semibold">Artworks</h1>
    <p className="text-text-secondary text-body">The artwork catalogue — pieces, artists and availability.</p>
  </section>
);

export default AdminArtworksPage;
