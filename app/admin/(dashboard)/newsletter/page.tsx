/** Newsletter — a placeholder until the frames for this section land. */
const AdminNewsletterPage = () => (
  <section className="flex flex-col gap-2">
    <h1 className="text-text-primary text-2xl font-semibold">Newsletter</h1>
    <p className="text-text-secondary text-body">Newsletter subscribers and their sign-up source.</p>
  </section>
);

export default AdminNewsletterPage;
