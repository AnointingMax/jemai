/** Overview — the console's landing screen. No modules designed for it yet. */
const AdminOverviewPage = () => (
  <section className="flex flex-col gap-2">
    <h1 className="text-text-primary text-2xl font-semibold">Overview</h1>
    <p className="text-text-secondary text-body">
      Catalogue, orders and audience at a glance. Nothing is wired up yet.
    </p>
  </section>
);

export default AdminOverviewPage;
