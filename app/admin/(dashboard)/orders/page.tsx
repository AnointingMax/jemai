/** Orders — a placeholder until the frames for this section land. */
const AdminOrdersPage = () => (
  <section className="flex flex-col gap-2">
    <h1 className="text-text-primary text-2xl font-semibold">Orders</h1>
    <p className="text-text-secondary text-body">Storefront orders, payments and fulfilment.</p>
  </section>
);

export default AdminOrdersPage;
