/** Placeholder landing screen; the admin sections are not built yet. */
const AdminHomePage = () => (
  <section className="flex flex-col gap-4">
    <h1 className="text-h2 font-heading">Admin</h1>
    <p className="text-body text-text-secondary max-w-prose">
      Internal tools live here. Nothing is wired up yet.
    </p>
  </section>
);

export default AdminHomePage;
